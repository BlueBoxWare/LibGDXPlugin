root
  selector
    parallel
      care urgentProb:0.8
              ######
              # Bar
              ######
      # Foo
      org.example.Playask # fully qualified task

                    ######
              # Bar
              ######
      # Foo

    randomSelector
      n<caret>tilSuccess  # find some trees to pee
        sequence
          bark times:"uniform,1,2"
          walk
          mark
      parallel policy:"selector"  # sleep with random timeout
        wait seconds:"triangular,2.5,5.5"
        rest
